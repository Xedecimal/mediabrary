<div id="tv-series">
<a href="{{series}}" id="scrape-tv-link">Search</a>
<a href="test.php?series={{series}}" target="_blank">Get More Episodes</a>
<item>
	<div class="tv-episode" title="{{fs_path}}" id="item:{{mov_id}}">
	{{have}}
	S{{med_season}}E{{med_episode}} - <a class="episode-link"
		href="tv/watch/{{med_series}}/{{fs_filename}}">{{med_title}}</a>
		{{med_date}}
	</div>
</item>
</div>