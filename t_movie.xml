<item>
	<div class="movie-item" title="{{med_path}}" id="item:{{med_id}}">
	<img class="movie-image" src="{{med_thumb}}" alt="{{med_title}}" />
	<input type="hidden" id="backdrop:{{med_id}}" value="{{med_backdrop}}" />
	</div>
</item>
